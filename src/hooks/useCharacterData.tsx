export function useCharacterData() {
  // const [character, setCharacterType] = useState({} as CharacterType);
  // useEffect(() => {
  //   const getUserCharacters = async () => {
  //     try {
  //       setCharacterType(data) => {}
  //       const response = await fetch(`http://localhost:8080/user/$username/characters`, {
  //         method: 'GET',
  //       });
  //       if (response.ok) {
  //         // If signin is successful, update the auth provider state
  //         const data = await response.json();
  //         console.log(`User ${data.username} created successfully`);
  //       } else {
  //         const errorData = await response.json();
  //         console.log(errorData);
  //         // Handle signin error here
  //         throw new Error(errorData.errorMessage);
  //       }
  //     } catch (err) {
  //       if (err instanceof Error) {
  //         throw new Error(err.message);
  //       } else {
  //         // Handle any other error that might occur during the API call
  //         throw new Error('Unknown error during sign up');
  //       }
  //     }
  //   };
  //   getUserCharacters();
  // }, []);
  // return character;
}
